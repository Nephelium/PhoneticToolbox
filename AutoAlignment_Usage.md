### 功能简介

本模块 (`auto_alignment`) 是一个基于 Montreal Forced Aligner (MFA) 的图形化自动标注工具。它旨在简化强制对齐（Forced Alignment）流程，帮助用户将音频与其对应的文本进行时间对齐，并自动生成包含音素和单词层级时间信息的 TextGrid 文件。

特别地，针对 MFA 对中文路径和文件名支持不佳的问题，本工具内置了**批量重命名与还原**功能，确保对齐过程的稳定性。

### 准备工作

在使用本工具前，请准备以下数据和模型文件：

1.  **音频与文本数据**：
    *   将所有音频文件（.wav）和对应的文本文件（.lab 或 .txt）放在同一个文件夹中。
    *   文件名应保持一致（例如 `001.wav` 和 `001.lab`）。
2.  **MFA 声学模型 (Acoustic Model)**：
    *   适用于目标语言的预训练声学模型文件（通常为 `.zip` 格式）。
3.  **MFA 发音字典 (Dictionary)**：
    *   目标语言的发音字典文件（通常为 `.dict` 或 `.txt` 格式），包含单词到音素的映射。

### 启动方式

在项目根目录下，双击运行 `auto_alignment/auto_alignment.bat` 脚本，或者在命令行中运行：

```bash
python auto_alignment/app/auto_annotation_main.py
```

### 界面参数说明

启动后，主界面包含以下配置项：

*   **MFA 声学模型路径**：点击“选择文件”按钮，加载准备好的 `.zip` 模型文件。
*   **MFA 字典路径**：点击“选择文件”按钮，加载准备好的字典文件。
*   **音频路径**：选择包含音频和文本数据的文件夹。
*   **输出路径**：设置对齐结果（TextGrid 文件）的保存位置。默认情况下，会自动同步为音频路径。

### 中文路径与文件名处理

由于底层 MFA 引擎对中文路径和文件名的支持有限，本工具提供了专门的辅助功能：

*   **批量转换中英文名 (Batch Rename)**：
    *   **功能**：将指定文件夹内的所有中文文件名转换为英文（或拼音/哈希），并生成一份映射日志 (`rename_log.json`)。
    *   **模式选择**：
        *   **另存为新文件夹**：保留原文件夹，创建一个后缀为 `_en` 的新文件夹进行处理（推荐）。
        *   **覆盖原文件夹**：直接修改原文件夹内的文件名。
*   **还原为中文名 (Restore Chinese Names)**：
    *   **功能**：根据 `rename_log.json` 日志文件，将文件名还原为原始中文名称。
    *   **智能还原**：该功能不仅会还原原始音频文件名，还会自动将 MFA 新生成的 TextGrid 文件也重命名为对应的中文名。

### 标准操作流程

建议按照以下步骤进行操作，以确保最佳成功率：

1.  **加载模型**：分别选择声学模型和字典文件。
2.  **选择数据**：选择包含原始数据的音频文件夹。
3.  **预处理（推荐）**：
    *   点击 **“批量转换中英文名”**。
    *   选择 **“另存为新文件夹”** 模式。
    *   工具会自动创建新文件夹，并自动将“音频路径”和“输出路径”更新为这个新的全英文路径文件夹。
4.  **开始对齐**：
    *   点击 **“开始对齐 (Start Alignment)”** 按钮。
    *   程序将调用 MFA 引擎在后台进行计算，下方日志框会显示进度。
    *   对齐完成后，会弹出成功提示。
5.  **结果还原**：
    *   对齐完成后，系统通常会询问是否自动还原文件名。
    *   如果需要手动还原，点击 **“还原为中文名”**，选择对应的 `rename_log.json` 文件即可。
    *   此时，新生成的 TextGrid 文件也将被重命名为中文，方便后续查看。

### 注意事项

*   **路径字符**：尽管工具提供了重命名功能，建议尽量将项目放置在全英文路径下，以避免某些底层依赖库加载失败。
*   **性能**：强制对齐是计算密集型任务，处理大量长音频时可能需要一定时间，请耐心等待进度条完成。
*   **日志**：如果遇到错误，请查看界面下方的黑色日志区域，其中包含了详细的错误信息，有助于排查模型不匹配或数据格式问题。
