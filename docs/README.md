# 文档目录约定

`docs/` 只存放项目级设计、实施计划和长期维护说明，不存放构建产物、测试输出或临时日志。

## 目录结构

- `plans/`：已经确认的设计与实施计划。

## 命名与维护

- 计划文件使用 `YYYY-MM-DD-<topic>.md`。
- 一个文件只描述一个相对独立的改动主题。
- 实施状态写在计划文件内；计划过时后标记为 `Superseded`，不直接删除。
- 构建报告、覆盖率 HTML、临时截图和运行日志应由工具生成到现有忽略目录，不提交到这里。

